you should have a D:\ folder

run alias once before starting

it will cryypic folder in D:\... in that folder run the registry file

Run the registry file before operating further.
It creates a permanent doskey with the address of the main function.
Henceforth the program can be called by using the keyword 'cryptologer' in the commmand line
from anywhere, followed by the respective flags.

Please dont mess with the location and contents of the directory.